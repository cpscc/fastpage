{{< layout }}
    {{$content}}
    <form method=post class="alert">
        {{message}}
        <br>
        <input class="button" type=submit value=Confirm>
    </form>
    {{/content}}
{{/ layout }}
